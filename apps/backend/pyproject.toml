[tool.ruff]
line-length = 100
target-version = "py312"
fix = false

[tool.black]
line-length = 100
target-version = ["py312"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[project.optional-dependencies]
dev = [
  "black==24.8.0",
]
